import './NavBar.css';

const NavBar = () => {
  return (
    <nav>
      <ul className="nav-list">
        <li>Inventory</li>
        <li>Favorites</li>
      </ul>
    </nav>
  );
};

export default NavBar;
